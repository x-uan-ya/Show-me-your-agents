{
  "name": "show-me-your-agents",
  "private": true,
  "version": "0.1.0",
  "description": "Root dev runner: starts the FastAPI backend and Vite frontend together.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "node scripts/dev.mjs backend",
    "dev:frontend": "node scripts/dev.mjs frontend",
    "setup": "node scripts/setup.mjs",
    "test:email": "node scripts/test-email.mjs",
    "test:dev": "node scripts/smoke-dev.mjs",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
